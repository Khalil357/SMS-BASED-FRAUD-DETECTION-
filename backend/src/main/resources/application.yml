ml:
  service:
    url: ${ML_SERVICE_URL:https://13.53.200.176}
    # Obtain ML_SERVICE_API_KEY from Barthez. Do not commit the key.
    api-key: ${ML_SERVICE_API_KEY:}

# Apple Message Filter associated-domain AASA serving (replace TEAM_ID in production).
app:
  apple:
    team-id: ${APPLE_TEAM_ID:TEAM_ID}
    app-bundle-id: ${APPLE_APP_BUNDLE_ID:com.example.secureSignal}
    extension-bundle-id: ${APPLE_EXTENSION_BUNDLE_ID:com.example.secureSignal.MessageFilterExtension}
